<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:sec="http://www.springframework.org/schema/security"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">


<!-- DB接続 -->
 <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
  <property name="driverClassName" value="${jdbc.driverClassName}" />
  <property name="url" value="${jdbc.url}" />
  <property name="username" value="${jdbc.username}" />
  <property name="password" value="${jdbc.password}" />
 </bean>

 <context:property-placeholder location="classpath:jdbc.propertie2.txt" />
    <sec:http auto-config="true">
        <sec:intercept-url pattern="/MyApp/**" access="ROLE_ADMIN"/>
        <sec:form-login login-page="/login.jsp" default-target-url="/LoginList"
                authentication-failure-url="/error.jsp"/>
        <sec:logout logout-url="/logout"
                logout-success-url="/login.jsp" />
        <sec:access-denied-handler error-page="/error.jsp" />
    </sec:http>

     <sec:authentication-manager>
  <sec:authentication-provider>
   <sec:jdbc-user-service data-source-ref="dataSource"
     users-by-username-query="
      select loginUser ,loginUserPass ,'true'
      from tbLoginUser
      where loginUser = ?"

     authorities-by-username-query="
      select loginUser ,'ROLE_ADMIN'
      from tbLoginUser
      where loginUser = ?"
   />
  </sec:authentication-provider>
 </sec:authentication-manager>
</beans>
