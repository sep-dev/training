<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:sec="http://www.springframework.org/schema/security"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- databaseの接続とか -->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
	</bean>

	<context:property-placeholder location="classpath:jdbc.properties" />


	<!-- security -->
	<sec:http auto-config="true" create-session="ifRequired" access-denied-page="/erro403">
		<sec:intercept-url pattern="/haken/**" access="ROLE_ADMIN"/>
		<sec:form-login login-page="/" default-target-url="/haken/top"
				authentication-failure-url="/error"/>
		<sec:logout logout-url="/logout"
				logout-success-url="/"
				invalidate-session="false"/>
		<sec:session-management
				invalid-session-url="/error403"
				session-authentication-error-url="/error403"
				session-fixation-protection="migrateSession" />
	</sec:http>

	<sec:authentication-manager>
		<sec:authentication-provider>
			<sec:jdbc-user-service data-source-ref="dataSource"
					users-by-username-query="
						select loginUser ,loginUserPass ,'true'
						from tblLoginUser
						where loginUser = ?"

					authorities-by-username-query="
						select loginUser ,'ROLE_ADMIN'
						from tblLoginUser
						where loginUser = ?"
			/>
		</sec:authentication-provider>
	</sec:authentication-manager>


</beans>